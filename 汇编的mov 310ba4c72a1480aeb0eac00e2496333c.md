# 汇编的mov

![微信图片_20260223140453_54_2.jpg](%E6%B1%87%E7%BC%96%E7%9A%84mov/%E5%BE%AE%E4%BF%A1%E5%9B%BE%E7%89%87_20260223140453_54_2.jpg)

### 先搞懂表格的 4 列含义

| **列名** | **含义** |
| --- | --- |
| 类型 | 操作数来自哪里：立即数（常数）、寄存器、存储器（内存） |
| 格式 | 汇编代码里怎么写这个寻址方式 |
| 操作数值 | 这个寻址方式最终得到的值怎么算（`R[r]`表示寄存器`r`的值，`M[addr]`表示内存地址`addr`处的值） |
| 名称 | 这种寻址方式的专业名称 |

---

### 逐行拆解（从简单到复杂）

### 1. 立即数寻址（Immediate）

- 格式：`$Imm`（比如 `$0x123`、`$-45`）
- 操作数值：`Imm`
- 解释：操作数就是指令里直接写的常数，不需要去内存或寄存器里取。
- 例子：`movl $0x4050, %eax` 里的 `$0x4050` 就是立即数寻址。

### 2. 寄存器寻址（Register）

- 格式：`r_a`（比如 `%eax`、`%rbp`）
- 操作数值：`R[r_a]`
- 解释：操作数就是寄存器里存的值。
- 例子：`movw %bp, %sp` 里的 `%bp` 和 `%sp` 都是寄存器寻址。

### 3. 绝对寻址（Absolute）

- 格式：`Imm`（比如 `0x4000`）
- 操作数值：`M[Imm]`
- 解释：用一个立即数作为**内存地址**，去这个地址读内存里的值。
- 例子：`movl 0x4000, %eax` 就是把地址`0x4000`处的 4 字节内存值读到`%eax`。

### 4. 间接寻址（Indirect）

- 格式：`(r_a)`（比如 `(%rax)`）
- 操作数值：`M[R[r_a]]`
- 解释：寄存器里的值是**内存地址**，去这个地址读内存。
- 例子：`movq (%rax), %rbx` 就是先看`%rax`里的值，把它当地址，去那个地址读 8 字节内存到`%rbx`。

### 5. 基址 + 偏移量寻址（Base + Displacement）

- 格式：`Imm(r_b)`（比如 `12(%rbp)`）
- 操作数值：`M[Imm + R[r_b]]`
- 解释：基址寄存器`r_b`的值 + 立即数偏移量`Imm`，得到内存地址，去读内存。
- 例子：`movl -12(%rbp), %eax` 就是`%rbp - 12`作为地址，读 4 字节内存到`%eax`（这在函数栈帧里非常常见）。

### 6. 变址寻址（Indexed）

- 格式：`(r_b, r_i)`（比如 `(%rax, %rcx)`）
- 操作数值：`M[R[r_b] + R[r_i]]`
- 解释：基址寄存器`r_b`的值 + 变址寄存器`r_i`的值，得到内存地址。
- 例子：`movb (%rax, %rcx), %al` 就是`%rax + %rcx`作为地址，读 1 字节内存到`%al`。

### 7. 比例变址寻址（Scaled Indexed）

- 格式：`(r_b, r_i, s)`（比如 `(%rax, %rcx, 4)`）
- 操作数值：`M[R[r_b] + R[r_i] · s]`
- 解释：基址`r_b` + 变址`r_i` × 比例因子`s`（s 只能是 1、2、4、8），得到地址。
- 例子：`movl (%rax, %rcx, 4), %edx` 就是`%rax + %rcx*4`作为地址，读 4 字节内存。这在数组访问里最常用：比如数组`int arr[]`，每个元素 4 字节，`arr[i]`的地址就是`arr_base + i*4`，正好对应这个寻址方式。

### 8. 带偏移的变址 / 比例变址寻址

- 格式：`Imm(r_b, r_i)` 或 `Imm(r_b, r_i, s)`
- 操作数值：`M[Imm + R[r_b] + R[r_i]]` 或 `M[Imm + R[r_b] + R[r_i] · s]`
- 解释：在变址 / 比例变址的基础上，再加一个立即数偏移量。
- 例子：`movq 8(%rbp, %rdx, 8), %rax` 就是`%rbp + %rdx*8 + 8`作为地址，读 8 字节内存（常用于结构体数组，`8`是结构体成员的偏移量）。

---

### 一句话总结

这张表就是 x86-64 里 **“怎么算内存地址”** 的全部套路：

- 最简单的：直接给地址（绝对、间接）
- 最常用的：基址 + 偏移（栈帧）、比例变址（数组）
- 最复杂的：基址 + 变址 + 比例 + 偏移（结构体数组）

下面是练习

![微信图片_20260223140454_55_2.jpg](%E6%B1%87%E7%BC%96%E7%9A%84mov/%E5%BE%AE%E4%BF%A1%E5%9B%BE%E7%89%87_20260223140454_55_2.jpg)

先提前明确 2 个核心基础，所有指令都围绕这两点：

1. 指令后缀`b/w/l/q` = 操作的数据长度：`b`=1 字节（8 位）、`w`=2 字节（16 位）、`l`=4 字节（32 位）、`q`=8 字节（64 位）
2. 操作数的核心规则：`$开头`= 立即数（固定常量）、`%开头`= 寄存器、`带括号()`= 访问内存（括号里算出来的是内存地址）
3. MOV 指令的本质：**把源操作数的数据复制到目的操作数，源数据不变，目的数据被覆盖**

---

### 逐行指令详解

### 1. `movl $0x4050,%eax`

- 后缀`l`：操作长度为 4 字节（32 位）
- 源操作数：`$0x4050`，立即数，也就是固定的常量值`0x4050`（十进制 16464）
- 目的操作数：`%eax`，64 位寄存器`%rax`的低 32 位
- 具体动作：把 32 位的立即数`0x00004050`写入`%eax`，**同时触发 x86-64 的特殊规则：以 32 位寄存器为目的的`movl`，会自动把对应 64 位寄存器的高 32 位清零**。执行后，完整的 64 位`%rax`的值为`0x0000000000004050`，而不是只修改低 32 位。
- 对应原文注释：`Immediate--Register, 4 bytes`（立即数→寄存器，4 字节操作）

### 2. `movw %bp,%sp`

- 后缀`w`：操作长度为 2 字节（16 位）
- 源操作数：`%bp`，64 位寄存器`%rbp`的低 16 位
- 目的操作数：`%sp`，64 位寄存器`%rsp`的低 16 位
- 具体动作：把`%bp`里的 2 字节数据，完整复制到`%sp`中。**因为是`w`后缀，只会修改`%rsp`的低 16 位，`%rsp`剩下的高 48 位完全保持不变，不会清零**，和`movl`的特殊规则无关。
- 对应原文注释：`Register--Register, 2 bytes`（寄存器→寄存器，2 字节操作）

### 3. `movb (%rdi,%rcx),%al`

- 后缀`b`：操作长度为 1 字节（8 位）
- 源操作数：`(%rdi,%rcx)`，x86 的「基址 + 变址内存寻址」，意思是：先计算内存地址 = `%rdi`的值 + `%rcx`的值，括号代表 “访问这个地址对应的内存单元”，也就是从这个内存地址里取 1 字节的数据。
- 目的操作数：`%al`，64 位寄存器`%rax`的低 8 位
- 具体动作：计算`rdi+rcx`得到内存地址，从这个地址的内存中取出 1 字节，复制到`%al`寄存器。**只会修改`%al`，`%rax`剩下的高 56 位完全不变**。
- 对应原文注释：`Memory--Register, 1 byte`（内存→寄存器，1 字节操作）

### 4. `movb $-17,(%rsp)`

- 后缀`b`：操作长度为 1 字节
- 源操作数：`$-17`，立即数，1 字节有符号数`17`（对应的 8 位补码是`0xEF`）
- 目的操作数：`(%rsp)`，访问`%rsp`的值对应的内存地址（也就是栈顶的内存单元）
- 具体动作：把 1 字节的常量`17`，写入到`%rsp`指向的栈顶内存中。因为目的是内存，没有寄存器高位清零的说法，就是单纯的内存写入。
- 对应原文注释：`Immediate--Memory, 1 byte`（立即数→内存，1 字节操作）

### 5. `movq %rax,-12(%rbp)`

- 后缀`q`：操作长度为 8 字节（64 位）
- 源操作数：`%rax`，完整的 64 位通用寄存器
- 目的操作数：`12(%rbp)`，x86 的「基址 + 偏移内存寻址」，意思是：内存地址 = `%rbp`的值 - 12，也就是栈帧基址往下偏移 12 字节的内存位置（CSAPP 后续讲栈帧、函数调用会高频用到这个寻址方式）
- 具体动作：把`%rax`里完整的 8 字节数据，复制到`%rbp-12`对应的内存地址中，一次性写入 8 个字节。
- 对应原文注释：`Register--Memory, 8 bytes`（寄存器→内存，8 字节操作）

---

### 最后给你串个总结

这 5 条指令，其实是 CSAPP 给你展示了 MOV 指令的 5 种合法的「源 - 目的」组合，覆盖了立即数、寄存器、内存三类操作数的搭配，同时也对应了 4 种不同的操作长度。